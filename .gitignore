node_modules/
__pycache__/
.mimosa/
data/*.db
data/*.db-journal
backend/data/*.db
backend/data/*.db-journal
my-backup*.db
*.before-restore
dist/
*.log
.env.local

# Agent 规划/会话状态，非项目内容
.zcode/

# 数据库 Markdown 备份（docs/database-backup.md）按项目所有者要求纳入版本管理；
# 原始 .db 文件仍由上面的规则排除。该文件含真实个人记账数据。
