node_modules/
__pycache__/
.mimosa/
data/*.db
data/*.db-journal
backend/data/*.db
backend/data/*.db-journal
my-backup*.db
*.before-restore
dist/
*.log
.env.local

# Agent 规划/会话状态，非项目内容
.zcode/

# 数据库 Markdown 备份：含个人记账数据（持仓、金额、日期），本仓库为公开仓库，
# 默认不入库。仅在确认仓库为私有后，用 git add -f docs/database-backup.md 显式提交。
docs/database-backup.md
